

$w.onReady(function () {
  // const tabStrip = $w('#tabs2');
  // const tabs = tabStrip.tabs;
  // const tabParam = wixLocation.query.tab;

  // if (typeof tabParam === 'undefined') {
  //   return; // no tab param, do nothing
  // }

  // const idx = Number(tabParam);

  // // Safety: ensure it's a valid index
  // if (!Number.isNaN(idx) && idx >= 0 && idx < tabs.length) {
  //   tabStrip.changeTab(tabs[idx]);
  // }
});
